plugins {
    id 'com.google.protobuf' version '0.8.5'
}

protobuf {
    protoc {
        artifact = 'com.google.protobuf:protoc:3.6.0'
    }
    generateProtoTasks {
        all()*.enabled = false
    }
}